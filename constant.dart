class AppConstants {
  static const eventsCollection = "events";
  static const usersCollection = "users";

  static const maxVideoSizeBytes = 5 * 1024 * 1024;
  static const maxVideoDurationSeconds = 15;
  static const maxImageSizeBytes = 300 * 1024;
}
